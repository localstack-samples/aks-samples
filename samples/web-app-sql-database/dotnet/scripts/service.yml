apiVersion: v1
kind: Service
metadata:
  name: vacation-planner-sql
  namespace: vacation-planner-sql
  labels:
    app: vacation-planner-sql
spec:
  type: ClusterIP
  selector:
    app: vacation-planner-sql
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
