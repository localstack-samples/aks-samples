apiVersion: v1
kind: Service
metadata:
  name: vacation-planner-mongodb
  namespace: vacation-planner-mongodb
  labels:
    app: vacation-planner-mongodb
spec:
  type: ClusterIP
  selector:
    app: vacation-planner-mongodb
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
