apiVersion: v1
kind: Service
metadata:
  name: vacation-planner-appconfig
  namespace: vacation-planner-appconfig
  labels:
    app: vacation-planner-appconfig
spec:
  type: ClusterIP
  selector:
    app: vacation-planner-appconfig
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
