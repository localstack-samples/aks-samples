apiVersion: v1
kind: Service
metadata:
  name: vacation-planner-postgres
  namespace: vacation-planner-postgres
  labels:
    app: vacation-planner-postgres
spec:
  type: ClusterIP
  selector:
    app: vacation-planner-postgres
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
