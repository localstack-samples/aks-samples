apiVersion: apps/v1
kind: Deployment
metadata:
  name: vacation-planner-postgres-in-cluster
  namespace: vacation-planner-postgres-in-cluster
  labels:
    app: vacation-planner-postgres-in-cluster
spec:
  replicas: 3
  selector:
    matchLabels:
      app: vacation-planner-postgres-in-cluster
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: vacation-planner-postgres-in-cluster
    spec:
      nodeSelector:
        kubernetes.io/os: linux
      containers:
        - name: vacation-planner-postgres-in-cluster
          image: <your-registry>.azurecr.io/vacation-planner-postgres-in-cluster-dotnet:v1
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PG_HOST
              valueFrom:
                configMapKeyRef:
                  name: vacation-planner-postgres-in-cluster-config
                  key: PG_HOST
            - name: PG_PORT
              valueFrom:
                configMapKeyRef:
                  name: vacation-planner-postgres-in-cluster-config
                  key: PG_PORT
            - name: PG_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: vacation-planner-postgres-in-cluster-config
                  key: PG_DATABASE
            - name: PG_USER
              valueFrom:
                configMapKeyRef:
                  name: vacation-planner-postgres-in-cluster-config
                  key: PG_USER
            - name: LOGIN_NAME
              valueFrom:
                configMapKeyRef:
                  name: vacation-planner-postgres-in-cluster-config
                  key: LOGIN_NAME
            - name: DEBUG
              valueFrom:
                configMapKeyRef:
                  name: vacation-planner-postgres-in-cluster-config
                  key: DEBUG
            - name: PG_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: vacation-planner-postgres-in-cluster-secrets
                  key: PG_PASSWORD
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: vacation-planner-postgres-in-cluster-secrets
                  key: SECRET_KEY
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
