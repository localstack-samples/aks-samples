apiVersion: v1
kind: Service
metadata:
  name: vacation-planner-file
  namespace: vacation-planner-file
  labels:
    app: vacation-planner-file
spec:
  type: ClusterIP
  selector:
    app: vacation-planner-file
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
